package org.stratosphere.squad.planets;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativePlanetResourceIT extends PlanetResourceTest {

    // Execute the same tests but in native mode.
}